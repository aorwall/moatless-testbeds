apiVersion: v1
kind: Service
metadata:
  name: {{ testbed_id }}
  namespace: {{ namespace }}
spec:
  selector:
    job-name: {{ testbed_id }}
  {% if in_cluster %}
  type: ClusterIP
  {% else %}
  type: LoadBalancer
  externalTrafficPolicy: Local
  sessionAffinity: ClientIP
  {% endif %}
  ports:
    - name: http
      port: 8000
      targetPort: 8000
